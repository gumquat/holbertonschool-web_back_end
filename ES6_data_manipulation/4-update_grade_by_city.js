export default function updateStudentGradeByCity(students, city, newGrades) {
  const studentGradeByCity = students.filter((student) => student.location === city).map((student) => );
  return studentGradeByCity;
}